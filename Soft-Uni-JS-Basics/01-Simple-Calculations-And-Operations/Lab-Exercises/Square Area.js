/*3. Пресмятане на лице на квадрат.
Да се напише конзолна програма, която изважда цяло число от аргумента input и пресмята лицето на квадрат със страна "a".
Задачата е тривиално лесна: получавате число в аргумента на функцията, умножавате го само по себе си и печатате получения резултат на конзолата.*/

function squareArea(input) {
  let a = input;
  // let a = Number(input.shift());
  let result = a * a;
  console.log(`The result is ${result}.`);
}

squareArea(6);
