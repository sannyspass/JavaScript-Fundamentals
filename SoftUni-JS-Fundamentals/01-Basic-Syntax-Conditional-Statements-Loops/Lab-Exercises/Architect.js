function architectProjects (nameArchitect, projects) {
    let projectTime = projects * 3;
    console.log(`The architect ${nameArchitect} will need ${projectTime} hours to complete ${projects} project/s.`);
}

architectProjects("Sanny", 2);